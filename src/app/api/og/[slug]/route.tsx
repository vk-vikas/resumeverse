import { NextResponse } from 'next/server';

export async function GET() {
  // Placeholder: OG image generation (Task 13)
  return NextResponse.json({ message: 'OG image endpoint — coming soon' }, { status: 501 });
}
