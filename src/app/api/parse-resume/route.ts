import { NextResponse } from 'next/server';

export async function POST() {
  // Placeholder: AI resume parsing (Task 5)
  return NextResponse.json({ message: 'Parse resume endpoint — coming soon' }, { status: 501 });
}
